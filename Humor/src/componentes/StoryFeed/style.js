import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    
    atividadesWrapper: {
        flexDirection: 'row',
        alignItems: 'center',
        paddingVertical: 3
    },
    Elipse: {
        paddingHorizontal: 8
    }
})

export default styles;