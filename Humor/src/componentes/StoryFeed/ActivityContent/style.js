import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    atividadesContent: {
        
        flexDirection: 'row',
        alignItems: 'center'
    },
    atividadesText: {
        fontSize: 12,
        color: 'black',
        lineHeight: 18,
        paddingLeft: 8
    }
})

export default styles;