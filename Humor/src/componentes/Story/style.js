import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    
    atividadesWrapper: {
        flexDirection: 'row',
        marginLeft: 28,
        marginTop: 16,
        alignItems: 'center',
    },
    Elipse: {
        marginRight: 8
    }
})

export default styles;